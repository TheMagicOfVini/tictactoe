You work in /Users/tmov/repos/cryoport/tictactoe on branch `fix/e6-s7-restrict-cors-and-protect-write-endpoints`. Read /Users/tmov/repos/cryoport/tictactoe/CLAUDE.md first. It defines a 7-step "Fix a TODO item" process. You do ONLY step 2 (Check). Do not edit specs, code, tests or `sdd/TODO.md`. Do not commit or change branches.

The target item is the first unchecked item in `sdd/TODO.md`: the E6 story entry "E6-S7 Restrict CORS and protect write endpoints" (`sdd/32-e6-s7-restrict-cors-and-protect-write-endpoints.md`), with two criteria: "CORS origins come from configuration instead of `*`" and "DELETE (and ideally direct PUT) require an admin credential or are removed from the public API". No story section above the E6 heading lists a gap for E6-S7. The E4-S4 item in the TODO mentions E6-S7 for the protection of PUT.

Do step 2:
- Read the E6-S7 story, `sdd/00-overview.md` (code map), and the story specs that E6-S7 touches: at least `sdd/19-e4-s6-allow-cross-origin-requests.md`, `sdd/17-e4-s4-update-a-player-s-stats.md`, `sdd/18-e4-s5-delete-a-player.md`, and any spec about local run, Docker or deployment (E5-S1, E5-S2, E5-S5) that names environment settings.
- Read the code: `backend/config/initializers/cors.rb`, `backend/config/routes.rb`, `backend/app/controllers/`, `backend/config/environments/`, `backend/Gemfile`, Docker and compose files, `.env` files or examples, backend request specs and helpers. In `frontend/src/`, find every API call and confirm which HTTP verbs and URLs the client uses (does it still call PUT or DELETE?), and how it gets the API base URL.
- Pick the story spec that step 3 (Spec) should rewrite. The E6 story is the TODO item, but its target behavior belongs in the E4 specs. Recommend which spec(s) to rewrite, with reasons.

Completion criterion: each E6-S7 criterion is confirmed or refuted as a current gap with a `file:line`. Every gap that E6-S7 covers but the TODO does not list is noted (for example: a CORS spec that states `*`, routes that expose PUT or DELETE, no auth on any endpoint, specs that describe public PUT/DELETE). Note that the item has no story section with a gap reference. Also list the design choices that the user must make before step 3, with the facts that bear on each (for example: remove PUT and DELETE from the public API, or protect them with an admin token from an env var; which env var name and default for CORS origins in development, test and production). Also note the test environment: how backend tests run (try `rbenv exec bundle exec rspec` in `backend/` and report the last lines), and that frontend tests run with `CI=true npm test` in `frontend/`.

Write your findings to `plans/e6-s7-restrict-cors-and-protect-write-endpoints-check.md` (short markdown, sections: TODO gaps, Gaps not in TODO, Story references, Specs to rewrite, Design choices, Test environment). Write in ASD-STE100 style: short sentences, active voice, simple words. Never use emdashes. Return a brief summary, with the design choices in full.
